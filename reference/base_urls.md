# Base URLs used in oncoEnrichR for linking out to external databases

Base URLs used in oncoEnrichR for linking out to external databases

## Usage

``` r
base_urls
```

## Format

A list of characters strings with base URLs
