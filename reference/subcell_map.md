# Format for subcell map object

Format for subcell map object

## Usage

``` r
subcell_map
```

## Format

A list with three elements, a data frame with subcellular map identities
(go terms, UniProt ids etc), a data frame with styled UI elements, and
an SVG image of the subcellular map
